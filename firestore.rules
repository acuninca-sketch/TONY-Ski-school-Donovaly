rules_version = '2';
<<<<<<< HEAD

service cloud.firestore {
  match /databases/{database}/documents {

    // This rule allows anyone with your Firestore database reference to view, edit,
    // and delete all data in your Firestore database. It is useful for getting
    // started, but it is configured to expire after 30 days because it
    // leaves your app open to attackers. At that time, all client
    // requests to your Firestore database will be denied.
    //
    // Make sure to write security rules for your app before that time, or else
    // all client requests to your Firestore database will be denied until you Update
    // your rules
    match /{document=**} {
      allow read, write: if request.time < timestamp.date(2025, 9, 23);
    }
  }
}
=======
service cloud.firestore {
  match /databases/{database}/documents {

    // Len admin alebo superadmin majú prístup k settings
    match /settings/{docId} {
      allow read, write: if request.auth != null && (request.auth.token.admin == true || request.auth.token.superadmin == true);
    }

    // Len admin alebo superadmin majú prístup k rezerváciám
    match /rezervacie/{docId} {
      allow read, write: if request.auth != null && (request.auth.token.admin == true || request.auth.token.superadmin == true);
    }

    // Kolekcia s rolami
    match /roles/{docId} {
      // Čítanie rolí len adminom/superadminom
      allow read: if request.auth != null && (request.auth.token.admin == true || request.auth.token.superadmin == true);

      // Zapisovať do rolí môže iba superadmin
      allow write: if request.auth != null && request.auth.token.superadmin == true;
    }
  }
}
>>>>>>> 262a2a841992b252496e8001713400646be61e06
